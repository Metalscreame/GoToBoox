package config

import(
	_ "github.com/lib/pq"
)
const (
	DB_USER     = "postgres" 		// replace it with your username for local machine
	DB_PASSWORD = "root"	 		// replace it with your passwrod for local machine
	DB_NAME     = "gotoboox"		// not need to replace
)

